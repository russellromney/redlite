---
title: Redlite
description: SQLite-backed Redis-compatible embedded key-value store in Rust
template: splash
hero:
  tagline: A Redis-compatible key-value store backed by SQLite. Embedded-first, persistent, zero config.
  image:
    file: ../../assets/houston.webp
  actions:
    - text: Get Started
      link: /getting-started/introduction/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/russellromney/redlite
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Why Redlite?

<CardGrid stagger>
	<Card title="Embedded-first" icon="laptop">
		Use as a library in your Rust application. No separate server process needed.
	</Card>
	<Card title="Disk is cheap" icon="document">
		Persistent storage without Redis's memory constraints. SQLite handles the heavy lifting.
	</Card>
	<Card title="Redis compatible" icon="puzzle">
		Existing Redis clients just work. Drop-in replacement for most use cases.
	</Card>
	<Card title="Zero config" icon="setting">
		ACID transactions, durability, and reliability out of the box with SQLite.
	</Card>
</CardGrid>

## Quick Example

```rust
use redlite::Db;

// Open a persistent database
let db = Db::open("mydata.db")?;

// Set and get values
db.set("hello", b"world", None)?;
let value = db.get("hello")?;
// Some(b"world")
```

Or run as a server and use `redis-cli`:

```bash
./redlite --db=mydata.db
```

```bash
redis-cli -p 6767 SET foo bar
redis-cli -p 6767 GET foo
# "bar"
```
